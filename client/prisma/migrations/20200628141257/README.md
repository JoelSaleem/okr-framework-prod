# Migration `20200628141257`

This migration has been generated by JoelSaleem at 6/28/2020, 2:12:57 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."KeyResult" DROP COLUMN "createdAt",
ADD COLUMN "createdAt" integer  NOT NULL ;

ALTER TABLE "public"."Objective" DROP COLUMN "createdAt",
ADD COLUMN "createdAt" integer  NOT NULL ;

ALTER TABLE "public"."User" DROP COLUMN "createdAt",
ADD COLUMN "createdAt" integer  NOT NULL ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200627141230..20200628141257
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource postgres {
   provider = "postgres"
-  url = "***"
+  url = "***"
 }
 // This "Example" model has been generated for you by Nexus.
 // Nexus does this when you do not have any models defined.
@@ -11,9 +11,9 @@
 model Objective {
   id              Int         @id @default(autoincrement())
   title           String
   description     String?
-  createdAt       DateTime
+  createdAt       Int
   user            User        @relation(fields: [userId], references: [id])
   userId          Int
   keyResults      KeyResult[]
   parentObjective Objective?  @relation(fields: [parentId], references: id)
@@ -24,9 +24,9 @@
 model KeyResult {
   id          Int        @id @default(autoincrement())
   title       String
   description String?
-  createdAt   DateTime
+  createdAt   Int
   target      Int
   current     Int
   user        User       @relation(fields: [userId], references: [id])
   userId      Int
@@ -38,9 +38,9 @@
   id         Int         @id @default(autoincrement())
   email      String      @unique
   password   String
   objectives Objective[]
-  createdAt  DateTime
+  createdAt  Int
   KeyResult  KeyResult[]
 }
 generator prisma_client {
```


